{% extends 'base.html.twig' %}

{% block title %}Student
{% endblock %}

{% block body %}
	<h1>Student</h1>

	<table class="table">
		<tbody>
			<tr>
				<th>Id</th>
				<td>{{ student.id }}</td>
			</tr>
			<tr>
				<th>FisrtName</th>
				<td>{{ student.FisrtName }}</td>
			</tr>
			<tr>
				<th>LastName</th>
				<td>{{ student.LastName }}</td>
			</tr>
			<tr>
				<th>NumEtud</th>
				<td>{{ student.NumEtud }}</td>
			</tr>
		</tbody>
	</table>

	<a class="btn btn-primary" href="{{ path('student_index') }}">back to list</a>


	<a class="btn btn-primary" href="{{ path('student_edit', {'id': student.id}) }}">edit</a>


	{{ include('student/_delete_form.html.twig') }}
{% endblock %}
